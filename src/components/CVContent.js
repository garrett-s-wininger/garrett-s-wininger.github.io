// @flow
'use strict';
import "./cvContent.css";
import * as React from 'react';

const CVContent = (): React.MixedElement => {
    return (
        <div className="cvContent">
            <p>CV Content</p>
        </div>
    );
};

export default CVContent;