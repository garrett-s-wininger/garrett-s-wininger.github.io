name: GitHub Page Deployment

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  main:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2

    - name: Configure NodeJS v10
      uses: actions/setup-node@v2
      with:
        node-version: 10
        cache: 'npm'
  
    - name: Install Project Dependencies via NPM
      run: npm ci

    - name: Typecheck Available JS Code
      run: npm run typecheck

    - name: Execute Jest JS Test Runner
      run: npm test -- --watchAll=false

    - name: Configure SSH Agent for GitHub Authentication
      if: ${{ github.event_name == 'push' }}
      env:
        SSH_AUTH_SOCK: /tmp/ssh_agent.sock
      run: |
        ssh-agent -a $SSH_AUTH_SOCK > /dev/null
        ssh-add - <<< "${{ secrets.GARRETT_S_WININGER_GITHUB_PAGE_SSH_PRIVATE_KEY }}"

    - name: Configure Git Identity Parameters
      if: ${{ github.event_name == 'push' }}
      run: |
        git config user.email "garrett_s_wininger_github_page_deployment_ci_action@example.com"
        git config user.name "Garrett S Wininger GitHub Page CI Deployment Action"

    - name: Deploy to GitHub Pages
      if: ${{ github.event_name == 'push' }}
      run: npm run deploy
